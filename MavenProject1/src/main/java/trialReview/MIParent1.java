package trialReview;

public interface MIParent1 {       
/*Object cannot be created in interface and abstract class. Classes does not support multiple inheritance. 
 * Hence we used interface in multiple inheritance.
 */
 void display1();
}
